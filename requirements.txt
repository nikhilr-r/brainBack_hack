# 🏦 BrainBack.AI — BankBot (Fully Offline Elite Architecture)
# Team AlgoNexus | PS-02 | Hackathon Edition
# Install: pip install -r requirements.txt

# ── Core Backend
flask==3.0.3
numpy==1.26.4
scipy==1.12.0
soundfile==0.12.1

# ── Elite STT (Whisper + Noise Reduction)
faster-whisper==1.0.3
noisereduce==3.0.1
huggingface_hub[hf_xet]

# ── Elite RAG (FAISS + Embeddings)
sentence-transformers==3.0.1
faiss-cpu==1.8.0

# ── Elite TTS (Sherpa-ONNX)
# High-fidelity offline Indic speech
sherpa-onnx==1.10.15

# ── Basic TTS (Fallback)
pyttsx3==2.99

# ── 💡 NOTE: LLM (Ollama) is a standalone binary.
# 1. Install from: https://ollama.com/download
# 2. Run: ollama pull gemma2:2b
# 3. Then run: py run.py
